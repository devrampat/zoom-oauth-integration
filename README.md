# zoom-oauth-integration

Zoom OAuth POC — a small full-stack example that demonstrates connecting a frontend React app to Zoom via OAuth using a minimal Express backend.

Prerequisites
- Node.js (v16+ recommended)
- npm
- A Zoom OAuth app (Client ID, Client Secret, Redirect URI)

Setup

1. Install dependencies

- Frontend:
   ```sh
   cd frontend
   npm install